Step 8 - Wrap it all up
=======================

Wrap the whole workflow into an `analyse` method and 
create a `main` method so that the script can be properly
called from the command line.


.. literalinclude:: simple_frap_8.py
   :language: python
   :emphasize-lines: 134-161
   :linenos:

Previous: :doc:`Step 7 <simple_frap_7>`
