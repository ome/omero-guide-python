channels:
  - conda-forge
  - ome
dependencies:
  - matplotlib
  - scikit-image
  - scipy
  - pip
  # ome: minimal dependencies required to use the Python API
  - omero-py
  # dependencies to use for zarr work
  - dask
  - dask-image
  - imageio
  - requests
  - zarr
