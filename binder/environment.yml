channels:
  - conda-forge
  - ome
dependencies:
  - pip
  # ome: minimal dependencies required to use the Python API
  - omero-py
  - pandas
  - pip:
    - ome-zarr
    - dask-image
    - stardist
    - tensorflow
